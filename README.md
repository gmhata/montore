# MONTORE（モントレ）

問診トレーニングシステム - Version 4.00

## 🎯 プロジェクト概要

**MONTORE**（モントレ）は、医療従事者向けの問診スキル訓練システムです。
OpenAI Realtime APIを活用したリアルタイム音声対話により、実践的な問診シミュレーションを提供します。

- **プロジェクト名**: MONTORE (MONshin TRAining + mOnitoRE)
- **バージョン**: 4.00
- **ベース**: Ver3.57からの新規独立プロジェクト

## 🌍 環境構成

### 本番環境（Production）
- **ブランチ**: `main`
- **Firebase プロジェクト**: `ver6-trainer-dev`（Ver3と共有）
- **Cloud Run サービス**: `montore`（Ver3の`ver6-trainer`とは別）
- **ビルド設定**: `cloudbuild-dev.yaml`
- **URL**: https://montore-xxxxx-an.a.run.app
- **用途**: 実際の教育・訓練環境

⚠️ **注意**: Ver3プロジェクト内で運用しますが、Cloud Runサービスは完全に分離されています。

## 🚀 デプロイフロー

```
main ブランチ
    ↓ (コード修正・機能追加)
    ↓ git add .
    ↓ git commit -m "説明"
    ↓ git push origin main
    ↓ → Cloud Build (cloudbuild-dev.yaml)
    ↓ → montore (本番) にデプロイ
    ↓
本番環境に反映
```

## 📋 開発手順

### コード修正・機能追加の流れ

```bash
# mainブランチで作業
git checkout main

# 最新の状態に更新
git pull origin main

# コードを編集
# ...

# コミット＆プッシュ
git add .
git commit -m "feat: 新機能の説明"
git push origin main

# → 自動的に montore (本番) にデプロイされる
```

**注意**: mainブランチへのプッシュは即座に本番環境に反映されます。

## 🔧 初回セットアップ

### 1. Firebase プロジェクト設定

Firebase Console で以下を設定：
- Firestore Database（asia-northeast1）
- Authentication（メール/パスワード）
- Webアプリ登録

### 2. Firebase 設定の更新

`public/auth.js` のfirebaseConfigを更新：

```javascript
const firebaseConfig = {
  apiKey: "YOUR_API_KEY",
  authDomain: "montore.firebaseapp.com",
  projectId: "montore",
  storageBucket: "montore.firebasestorage.app",
  messagingSenderId: "YOUR_SENDER_ID",
  appId: "YOUR_APP_ID"
};
```

### 3. Cloud Build トリガーの設定

Google Cloud Console で以下のトリガーを設定：

- **名前**: `deploy-production`
- **イベント**: ブランチにプッシュ
- **ソース**: `^main$`
- **ビルド構成**: `cloudbuild-dev.yaml`
- **プロジェクト**: `montore`

### 4. Firebase 認証ドメインの追加

Firebase Console > Authentication > Settings

承認済みドメインに追加:
- `montore-xxxxx-an.a.run.app`（Cloud RunのURL）

### 5. Secret Manager設定

OpenAI API Keyを Secret Manager に登録：

```bash
gcloud secrets create OPENAI_API_KEY \
  --project=montore \
  --data-file=- <<< "sk-proj-YOUR_API_KEY"
```

## 🛠️ 技術スタック

- **Backend**: Node.js 20+ / Express
- **Frontend**: Vanilla JavaScript
- **Database**: Cloud Firestore
- **Auth**: Firebase Authentication
- **AI**: OpenAI GPT-4o Realtime API / GPT-4o-mini
- **Hosting**: Google Cloud Run (Container)
- **CI/CD**: Google Cloud Build
- **Storage**: Google Cloud Storage

## 📝 環境変数

### 本番環境（ver6-trainer-dev プロジェクト）
- `FIREBASE_PROJECT_ID`: ver6-trainer-dev
- `FIRESTORE_PROJECT_ID`: ver6-trainer-dev
- `APP_VERSION`: 4.00
- `ASSETS_BUCKET`: ver6-trainer-recordings
- `OPENAI_API_KEY`: Secret Manager から取得（Ver3と共有）

## 🎓 主要機能

### 1. カスタム患者システム
- 教員が自由に患者プロフィールを作成
- 年齢、主訴、既往歴、現病歴などを設定
- システムプロンプトでAI患者の振る舞いを制御

### 2. リアルタイム音声対話
- OpenAI Realtime APIによる自然な会話
- WebRTC接続によるリアルタイム音声認識
- 学生は実際の問診のように話しかけられる

### 3. 9項目ルーブリック評価
1. 導入（名乗り/挨拶）
2. 主訴
3. OPQRST法
4. ROS & Red Flag
5. 医療・生活歴
6. 受診契機
7. バイタルサイン/現症（自動判定）
8. 身体診察（自動判定）
9. 進行

各項目0-1-2点（18点満点→100点換算）

### 4. AIコーチ機能
- GPT-4o-miniによる具体的な改善アドバイス
- 学生用と管理者用の詳細分析

### 5. セッション管理
- 全会話ログの保存
- タイムスタンプ付き文字起こし
- 評価結果とスコアの記録

## 📚 ドキュメント

- **技術仕様書**: [TECHNICAL_SPECIFICATION_v3.57.md](./TECHNICAL_SPECIFICATION_v3.57.md)
  - システムアーキテクチャ
  - 評価基準の詳細
  - コスト構造
  - 変更履歴

## 🔑 管理者権限

管理者メール: `gmhata@gmail.com`

このメールアドレスは `server.js` の `isAdminEmail()` 関数で自動的に管理者権限が付与されます。

## 🔍 トラブルシューティング

### デプロイが失敗する場合

1. Cloud Build のログを確認
   - https://console.cloud.google.com/cloud-build/builds?project=montore

2. Docker イメージのキャッシュ問題の場合
   - `--no-cache` フラグが設定されているか確認

### 認証エラーが出る場合

1. Firebase 認証ドメインに Cloud Run の URL が追加されているか確認
2. `public/auth.js` の firebaseConfig が正しいか確認

### 500エラーが出る場合

1. Cloud Run のログを確認
2. 環境変数（特に FIRESTORE_PROJECT_ID）が正しく設定されているか確認
3. Firestore のセキュリティルールを確認

## 📞 サポート

問題が発生した場合は、GitHub Issues で報告してください。

## 📖 Ver3との関係

MONTOREはVer3.57をベースとした完全独立プロジェクトです。

- **Ver3環境**: 既存の`ver6-trainer-dev`プロジェクトとして継続運用
- **MONTORE環境**: 新しい`montore`プロジェクトとして独立運用
- **互いに影響なし**: データベース、認証、デプロイが完全分離

Ver3で実証された機能と設計をベースに、新機能の実験と開発を行います。

## 🎉 Ver4.00の特徴

- Ver3.57の安定性を継承
- 完全独立環境での運用
- 新機能の追加準備完了
- クリーンなプロジェクト構造
